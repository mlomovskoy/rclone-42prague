# Machine-local overrides for 42-common.sh's defaults. Not version-controlled,
# and never touched again by 42sync_install.sh once this file exists -- edit
# it (here, at $CONFIG_PATH/42-common.local.sh) any time, on any machine, and
# it sticks across reinstalls of the tools themselves.
#
# Sourced AFTER every default below is already set, so a plain reassignment
# here is enough to override it. Everything you leave commented out just
# keeps its default. The values shown are those defaults, for reference.

# LOCAL_PATH="$HOME_PATH/Projects"
# REMOTE_PATH="gdrive:_projects-sync-rclone"

# CONFIG_PATH="$HOME_PATH/.config/_projects-sync-rclone"
# FILE_FILTERS="$CONFIG_PATH/filters/projects-filters.txt"
# PROJECT_FILTERS="$CONFIG_PATH/filters/projects-local.txt"

# STATE_PATH="$HOME_PATH/.local/state/_projects-sync-rclone"
# KEEP_LOGS=20

# BIN_PATH="$HOME_PATH/bin"
